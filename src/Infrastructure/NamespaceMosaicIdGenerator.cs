using System.Numerics;
using Symnity.Core.Format;

namespace Symnity.Infrastructure
{
    public class NamespaceMosaicIdGenerator
    {
        /**
         * @param {byte[]} nonce Mosaic nonce
         * @param {byte[]} ownerAddress Address
         * @returns {int[]} mosaic Id
         */
        
        /*public static BigInteger MosaicId(byte[] nonce, byte[]  ownerAddress){
            return IdGenerator.GenerateMosaicId(nonce, ownerAddress);
        }*/
    }
}